---
collections:
  # Explicitly declaring the platform dependency handles mapping conflicts
  - name: ansible.platform
    version: ">=2.5.0"

  # The target collection you are installing
  - name: infra.support_assist
    version: ">=1.1.3"
