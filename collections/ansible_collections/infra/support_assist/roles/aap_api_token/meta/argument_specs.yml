---
argument_specs:
  main:
    short_description: Auto-generated specs for aap_api_token role - main entry point
    description:
      - Get's a token for AAP using username and password
    author:
      - Lenny Shirley
    options:
      aap_api_token_no_log:
        description: Authentication token (disabled by default)
        type: bool
        default: false
        version_added: 0.1.0-devel
      aap_controller_full_version:
        description: Variable used in main entry point
        type: str
        version_added: 0.1.0-devel
      aap_hostname:
        description: Hostname or IP address for AAP instance (configured with default value)
        type: str
        default: "{{ lookup('env', 'AAP_HOSTNAME') | default(lookup('env', 'CONTROLLER_HOST') | default(lookup('env', 'TOWER_HOST') | default(''))) }}"
        version_added: 0.1.0-devel
      aap_password:
        description: Password for authentication (configured with default value)
        type: str
        default: "{{ lookup('env', 'AAP_PASSWORD') | default(lookup('env', 'CONTROLLER_PASSWORD') | default(lookup('env', 'TOWER_PASSWORD') | default(''))) }}"
        version_added: 0.1.0-devel
      aap_token:
        description: Authentication token (configured with default value)
        type: str
        default: >-
          {{
            lookup('env', 'AAP_TOKEN') | default(
              lookup('env', 'CONTROLLER_OAUTH_TOKEN') | default(
                lookup('env', 'TOWER_OAUTH_TOKEN') | default('')
              )
            )
          }}
        version_added: 0.1.0-devel
      aap_username:
        description: Username for authentication (configured with default value)
        type: str
        default: "{{ lookup('env', 'AAP_USERNAME') | default(lookup('env', 'CONTROLLER_USERNAME') | default(lookup('env', 'TOWER_USERNAME') | default(''))) }}"
        version_added: 0.1.0-devel
      aap_validate_certs:
        description: SSL/TLS certificate (configured with default value)
        type: bool
        default: >-
          {{
            lookup('env', 'AAP_VALIDATE_CERTS') | default(
              lookup('env', 'CONTROLLER_VERIFY_SSL') | default(
                lookup('env', 'TOWER_VERIFY_SSL') | default('true')
              )
            ) | bool
          }}
        version_added: 0.1.0-devel
  clear_token:
    short_description: Auto-generated specs for aap_api_token role - clear_token entry point
    description:
      - Get's a token for AAP using username and password
    author:
      - Lenny Shirley
    options:
      aap_api_token_no_log:
        description: Authentication token (disabled by default)
        type: bool
        default: false
        version_added: 0.1.0-devel
      aap_controller_full_version:
        description: Variable used in clear_token entry point
        type: str
        version_added: 0.1.0-devel
      aap_hostname:
        description: Hostname or IP address for AAP instance (configured with default value)
        type: str
        default: "{{ lookup('env', 'AAP_HOSTNAME') | default(lookup('env', 'CONTROLLER_HOST') | default(lookup('env', 'TOWER_HOST') | default(''))) }}"
        version_added: 0.1.0-devel
      aap_password:
        description: Password for authentication (configured with default value)
        type: str
        default: "{{ lookup('env', 'AAP_PASSWORD') | default(lookup('env', 'CONTROLLER_PASSWORD') | default(lookup('env', 'TOWER_PASSWORD') | default(''))) }}"
        version_added: 0.1.0-devel
      aap_token:
        description: Authentication token (configured with default value)
        type: str
        default: >-
          {{
            lookup('env', 'AAP_TOKEN') | default(
              lookup('env', 'CONTROLLER_OAUTH_TOKEN') | default(
                lookup('env', 'TOWER_OAUTH_TOKEN') | default('')
              )
            )
          }}
        version_added: 0.1.0-devel
      aap_username:
        description: Username for authentication (configured with default value)
        type: str
        default: "{{ lookup('env', 'AAP_USERNAME') | default(lookup('env', 'CONTROLLER_USERNAME') | default(lookup('env', 'TOWER_USERNAME') | default(''))) }}"
        version_added: 0.1.0-devel
      aap_validate_certs:
        description: SSL/TLS certificate (configured with default value)
        type: bool
        default: >-
          {{
            lookup('env', 'AAP_VALIDATE_CERTS') | default(
              lookup('env', 'CONTROLLER_VERIFY_SSL') | default(
                lookup('env', 'TOWER_VERIFY_SSL') | default('true')
              )
            ) | bool
          }}
        version_added: 0.1.0-devel
...
