---
# users_mock scenario inventory.
# connection: local used throughout; gateway vars point at the mock server.
all:
  vars:
    ansible_connection: local
    gateway_hostname: "http://127.0.0.1:8000"
    gateway_username: "mock"
    gateway_password: "testpass"
    gateway_validate_certs: false
  children:
    gateway_under_test:
      hosts:
        localhost: {}
