---
- name: Verify — role_team_assignment created
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    gateway_hostname: "http://127.0.0.1:8000"
    gateway_username: "mock"
    gateway_password: "testpass"
    gateway_validate_certs: false
  tasks:
    - name: Confirm role_team_assignment exists
      ansible.builtin.debug:
        msg: "role_team_assignment created successfully"
      vars:
        ansible_connection: local
...
