---
- name: Verify — ui_plugin_routes created
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    gateway_hostname: "http://127.0.0.1:8000"
    gateway_username: "mock"
    gateway_password: "testpass"
    gateway_validate_certs: false
  tasks:
    - name: Get ui_plugin_route (direct)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-direct"
        state: exists
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: exists_result
      vars:
        ansible_connection: local

    - name: Assert ui_plugin_route was found and updated (direct)
      ansible.builtin.assert:
        that:
          - exists_result is not failed
          - exists_result.get('exists') | default(false) | bool
          - exists_result.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (direct)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (direct)."
      vars:
        ansible_connection: local

    - name: Get ui_plugin_route (persistent)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-persistent"
        state: exists
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: exists_result_persistent
      vars:
        ansible_connection: local

    - name: Assert ui_plugin_route was found and updated (persistent)
      ansible.builtin.assert:
        that:
          - exists_result_persistent is not failed
          - exists_result_persistent.get('exists') | default(false) | bool
          - exists_result_persistent.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (persistent)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (persistent)."
      vars:
        ansible_connection: local

    - name: Get ui_plugin_route (local)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-local"
        state: exists
        gateway_hostname: "{{ gateway_hostname }}"
        gateway_username: "{{ gateway_username }}"
        gateway_password: "{{ gateway_password }}"
        gateway_validate_certs: "{{ gateway_validate_certs }}"
      register: exists_result_local
      vars:
        ansible_connection: local

    - name: Assert ui_plugin_route was found and updated (local)
      ansible.builtin.assert:
        that:
          - exists_result_local is not failed
          - exists_result_local.get('exists') | default(false) | bool
          - exists_result_local.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (local)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (local)."
      vars:
        ansible_connection: local

- name: Verify — ui_plugin_routes created
  hosts: localhost
  connection: ansible.platform.http
  gather_facts: false
  vars:
    gateway_hostname: "http://127.0.0.1:8000"
    gateway_username: "mock"
    gateway_password: "testpass"
    gateway_validate_certs: false
    ansible_platform_use_persistent_connection: false
  tasks:
    - name: Get ui_plugin_route (direct)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-direct-hd"
        state: exists
      register: exists_result

    - name: Assert ui_plugin_route was found and updated (direct)
      ansible.builtin.assert:
        that:
          - exists_result is not failed
          - exists_result.get('exists') | default(false) | bool
          - exists_result.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (direct)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (direct)."

    - name: Get ui_plugin_route (persistent)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-persistent-hd"
        state: exists
      register: exists_result_persistent

    - name: Assert ui_plugin_route was found and updated (persistent)
      ansible.builtin.assert:
        that:
          - exists_result_persistent is not failed
          - exists_result_persistent.get('exists') | default(false) | bool
          - exists_result_persistent.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (persistent)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (persistent)."

    - name: Get ui_plugin_route (http direct)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-local-hd"
        state: exists
      register: exists_result_http_direct

    - name: Assert ui_plugin_route was found and updated (http direct)
      ansible.builtin.assert:
        that:
          - exists_result_http_direct is not failed
          - exists_result_http_direct.get('exists') | default(false) | bool
          - exists_result_http_direct.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (http direct)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (http direct)."

- name: Verify — ui_plugin_routes created
  hosts: localhost
  connection: ansible.platform.http
  gather_facts: false
  vars:
    gateway_hostname: "http://127.0.0.1:8000"
    gateway_username: "mock"
    gateway_password: "testpass"
    gateway_validate_certs: false
    ansible_platform_use_persistent_connection: true
  tasks:
    - name: Get ui_plugin_route (direct)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-direct-hp"
        state: exists
      register: exists_result

    - name: Assert ui_plugin_route was found and updated (direct)
      ansible.builtin.assert:
        that:
          - exists_result is not failed
          - exists_result.get('exists') | default(false) | bool
          - exists_result.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (direct)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (direct)."

    - name: Get ui_plugin_route (persistent)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-persistent-hp"
        state: exists
      register: exists_result_persistent

    - name: Assert ui_plugin_route was found and updated (persistent)
      ansible.builtin.assert:
        that:
          - exists_result_persistent is not failed
          - exists_result_persistent.get('exists') | default(false) | bool
          - exists_result_persistent.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (persistent)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (persistent)."

    - name: Get ui_plugin_route (http persistent)
      ansible.platform.ui_plugin_route:
        name: "molecule-mock-ui-route-local-hp"
        state: exists
      register: exists_result_http_persistent

    - name: Assert ui_plugin_route was found and updated (http persistent)
      ansible.builtin.assert:
        that:
          - exists_result_http_persistent is not failed
          - exists_result_http_persistent.get('exists') | default(false) | bool
          - exists_result_http_persistent.get('ui_plugin_route', {}).get('description') == "Updated mock UI route (http persistent)"
        fail_msg: "Verify: ui_plugin_route not found or description not updated (http persistent)."
...
