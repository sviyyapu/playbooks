ruff                    # Lint + format (replaces flake8, black, isort); config in pyproject.toml
mypy                    # Static type checking; config in pyproject.toml
types-requests          # Type stubs for the requests library (used by mypy)
types-PyYAML            # Type stubs for PyYAML / yaml (used by mypy)
pydoclint               # Docstring style enforcement; config in pyproject.toml
tox                     # Used for unit tests
requests
